<%= form_for(@nakshatra) do |f| %>
  <% if @nakshatra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nakshatra.errors.count, "error") %> prohibited this nakshatra from being saved:</h2>

      <ul>
      <% @nakshatra.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<p id="notice"><%= notice %></p>
<%= link_to new_nakshatra_path, class: "btn btn-circle btn-success btn-lg pull-right" do %>
  <i class="fa fa-plus"></i>
<%end%>

<h1 class="page-header ">Listing Casts</h1>

<table class="table table-striped table-hover table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Show</th>
      <th>Edit</th>
      <th>Delete</th>
    </tr>
  </thead>

  <tbody>
    <% @casts.each do |cast| %>
      <tr>
        <td><%= cast.name %></td>
        <td><%= link_to 'Show', cast %></td>
        <td>
          <%= link_to edit_cast_path(cast) do %>
            <i class="fa fa-pencil-square-o"></i>
          <%end%>

        </td>
        <td>
          <%= link_to cast, method: :delete, data: { confirm: 'Are you sure?' } do%>
            <i class="fa fa-trash-o"></i>
          <%end%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

